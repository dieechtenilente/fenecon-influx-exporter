name: Keep GitHub Actions Alive

on:
  schedule:
    - cron: "0 3 1 * *"  # runs once per month
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - name: Keepalive step
        run: echo "Keeping GitHub Actions active"
